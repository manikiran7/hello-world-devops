apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend
  labels:
    app: {{ .Release.Name }}-backend

spec:
  type: ClusterIP

  selector:
    app: {{ .Release.Name }}-backend

  ports:
    - name: http
      port: 3000
      targetPort: http
